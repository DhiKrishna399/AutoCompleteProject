README:

	FILES INCLUDED:

		binarySearchDeluxe.java - program that performs binary search in the list of words
		Autocomplete.java - combines Term and binarySearchDeluxe to mimic an autocomplete program
		Term.java - Uses the weighting of words based on prefix the typed
		log.txt
		README - (this file) text file with list of every file included
